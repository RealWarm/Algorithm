#include<iostream>
#include<algorithm>
#include<string>
#include<stack>
using namespace std;
stack<char> st;

int main(){
	string org, bmb; 
	cin >> org >> bmb;
	int be=bmb.size()-1; //bmb의 마지막 인덱스 
	while(1){
		
		
	}//while
		
	return 0;
}//main

//문자열이 폭발 문자열을 포함하고 있는 경우에, 
//모든 폭발 문자열이 폭발하게 된다. 
//남은 문자열을 순서대로 이어 붙여 새로운 문자열을 만든다.
//새로 생긴 문자열에 폭발 문자열이 포함되어 있을 수도 있다.
//폭발은 폭발 문자열이 문자열에 없을 때까지 계속된다.


// 설계 
// 문자열을 탐색하면서 폭탄의 끝문자와 같은게 있다면
// 폭탄과 같은 문자인지 파악한다. >> 맞다면 제거 한다. 
 















